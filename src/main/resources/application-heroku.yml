spring:
    datasource:
        dirverClassName: org.postgresql.Driver
        maxActive: 20
        maxIdle: 5
        minIdle: 2
        initialSize: 5
        removeAbandoned: true
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: none

app:
   cors:
      allowedOrigin: https://ilgt.github.io